# 이 파일을 terraform.tfvars 로 복사해 값을 채운다.
# terraform.tfvars 는 저장소에 올리지 않는다. 기본값이 있는 변수는 적지 않아도 된다(variables.tf).

# DMZ 허니팟 이미지. 비우면 DMZ 허니팟을 만들지 않는다 (README "DMZ 재구성" 3단계)
honeypot_dmz_ami = "ami-xxxxxxxxxxxxxxxxx"

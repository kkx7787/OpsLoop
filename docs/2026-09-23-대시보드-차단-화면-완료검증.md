# 대시보드 · 차단 목록 · 실시간 갱신 검증 (#26)

작업 브랜치: `feat/26-dashboard-blocklist` · 기준 커밋: `90d80b6`.
기존에 채택한 공통 디자인으로 S-02·S-06·S-10을 구현하고 console-a에 배포했다.

## 구현

| 항목 | 결과 |
|---|---|
| 대시보드 | 미판정 수·최장 경과·목표 임박/초과·5구간 경과 분포·오래된 8건·규칙/버전별 비조치율 |
| 차단 목록 | 활성·만료·해제 탭, 사유·사건 링크·만료 시각·집행 결과·요청자, 검색·25/50/100건 페이지 탐색 |
| 해제 | admin만 표시, 확인 후 기존 조치 API 호출. 서버에서 만료·선행 해제·근거 사건 변경을 409로 거부 |
| 실시간 | 사건·판정·조치 통보 시 관련 데이터 재조회. 재접속 시 끊긴 동안 놓친 변경도 재조회 |
| 연결 실패 | 웹소켓 단절은 재연결 안내, REST 실패는 마지막 조회 시각·기존 결과·재시도 표시. 오래된 차단 목록에서는 해제 비활성 |
| 기존 API | `/api/stats/summary`, `/api/blocklist` 응답 확장. 새 경로·DB 스키마·권한 추가 없음 |

미판정은 판정 기록이 없는 사건이다. 비조치율은 사건별 마지막 판정을 사용하고 미결은 분모에서 제외한다. 활성 차단 요청과 실제 집행 확인은 구별한다.

## 검증

| 검사 | 결과 |
|---|---|
| `cd console && npm test` | 28개 파일, 239개 시험 통과 |
| `npm run lint` · `npm run build` | 통과 |
| `python3 -m unittest discover -s app -p 'test*.py'` | 일반 시험 43개 통과, DB 환경 미지정 시 통합 시험 6개 건너뜀 |
| PostgreSQL 통합 시험 | console-a에서 별도 시험 패키지로 6개 모두 통과. 연결별 임시 테이블·`search_path=pg_temp` 사용, 운영 데이터·계정 생성 없음 |
| 브라우저 | 로컬 검증 데이터로 390px 모바일·1024px 태블릿·1440px 데스크톱 확인. 태블릿 목록 넘침 수정 |
| 브라우저 조작 | 차단 페이지 전환·검색 시 1쪽 복귀·해제 확인창 확인. 실제 해제 POST는 자동 시험에서 검증 |
| 실패·복구 | 로컬 웹소켓 단절 표시, REST 503 시 기존 결과 유지·해제 비활성, 재조회 복구 확인 |
| 배포 후 실제 DB | 새 집계 함수 실행·JSON 직렬화 통과. 경과 구간 합계·목표 분류·활성 차단 수 일치 |

2026-09-23 17:09 KST 읽기 전용 검증에서 미판정 431건, 목표 초과 427건, 임박 1건, 규칙/버전 12개, 활성 차단 요청 13건이었다. 이는 검증 시점의 값이며 이후 바뀐다.

## 배포

- 대상: console-a `/home/ops/opsloop`, 컨테이너 `opsloop-api`.
- 17:08 KST에 `app/main.py`, `app/dashboard.py`, 프런트 빌드를 함께 배포했다.
- 배포 전 화면은 `index-D0nRqqXb.js` / `style-BSNqEKB6.css`로 이전 디자인이었다. 개선된 디자인은 코드에 채택됐으나 서버 빌드에 반영되지 않았던 상태였다.
- 배포 후 파일은 `index-DpYzlkJX.js` / `style-YwH6yCkJ.css`이며, 실행 컨테이너의 빌드 파일을 확인했다.
- 로컬과 서버의 API·HTML·JS·CSS SHA-256이 일치했다. 새 HTML SHA-256: `098c3fb497f7a428745cec8c7a9ac4ae5841536fc1f3ee4233ea4416a35aefb7`.
- 관문 `http://192.168.70.254:8443/health` 정상. 배포 확인용 브라우저에는 로그인 세션이 없어 인증된 운영 UI 조작은 하지 않았다. 정적 파일도 세션 보호 대상이므로 비로그인 HTTP 요청은 정상적으로 `/login`으로 이동한다.
- 기존 로그인 화면은 서버 렌더링이며 이번 디자인 변경 범위에 포함하지 않았다.

백업: `/home/ops/opsloop/backups/issue26-20260923-1708/app-before.tar.gz`.
이전 이미지: `opsloop-api:before-issue26-20260923-1708`.

## 범위와 한계

- 차단 목록 검색·페이지 탐색은 기존 배열 API의 응답에서 처리한다. 서버 페이지 API는 추가하지 않았다.
- 웹소켓 단절만으로 DB·센서 장애를 단정하지 않는다. 실제 장애 구성요소별 진단은 이번 범위 밖이다.
- 차단 요청/해제 기록은 방화벽의 실제 적용 완료를 보장하지 않는다. 집행 확인 필드를 별도로 표시한다.
- 규칙·노드·감사 화면, 로그인 화면의 디자인 변경은 별도 작업이다.
